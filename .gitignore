# Credentials — never commit
.env
.env.*
!.env.example

# Local zone snapshots written by enable-detections.sh (may contain zone config)
backups/

# SentinelOne runner credentials — never commit
credentials.json
.sentinelone/

# OS junk
.DS_Store
